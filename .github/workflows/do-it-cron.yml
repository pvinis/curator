name: do-it-cron

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  do-it-cron:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: do it
        run: node index.js
